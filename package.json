{
  "name": "pingint",
  "version": "1.0.0",
  "description": "Server for handling ping requests using the ping library",
  "main": "ping.js",
  "scripts": {
    "start": "node ping.js"
  },
  "dependencies": {
    "ping": "^0.4.4"
  }
}
