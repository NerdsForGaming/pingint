{
  "name": "pingint",
  "version": "1.0.0",
  "description": "Server for handling ping requests using the ping library",
  "main": "ping.js", // Assuming your entry file is named index.js
  "scripts": {
    "start": "node ping.js" // Adjust the start command based on your entry file name
  },
  "dependencies": {
    "ping": "^0.4.4" // Replace with the appropriate version of the ping library
  }
}
